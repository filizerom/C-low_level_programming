#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - write first lower and upper letter
 *
 * Return: 0
 */

int main(void)
{
	char alph;

	for (alph = 'a'; alph <= 'z'; alph++)
		putchar(low);
	for (alph = 'A'; alph <= 'Z'; alph++)
		putchar(alph);
	putchar('\n');

	return (0);
}
